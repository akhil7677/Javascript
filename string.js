const name="hitesh"
const repoCount=50;
//now if we have to add strings we can simpy do
console.log(name+repoCount);
//but this way is bad of doing it
// what we will do is string INTERPOLATION (python wali f string)
//  this is done through by using the key present under
// the escape key

console.log(`hello my ${name} us and my repo count is ${repoCount}`);

